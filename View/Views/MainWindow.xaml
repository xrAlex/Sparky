<Window x:Class="View.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:View.Views"
        xmlns:ext="clr-namespace:View.Extension"
        xmlns:locl="clr-namespace:View.Localization"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Sparky" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        Background="Transparent" 
        DataContext ="{Binding MainWindowViewModel, Source={StaticResource Locator}, Mode=OneWay}"
        Height="330" Width="440">
    <Border 
        CornerRadius="10" 
        Background="{StaticResource LightBlack}" 
        BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="57*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="86*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border 
                Grid.Column="1" Grid.Row="1"
                CornerRadius="5"
                Background="White">
                <Grid>
                    <ListBox HorizontalContentAlignment="Left"
                        ItemsSource="{Binding Screens, Mode=OneWay}"
                        VerticalAlignment="Top"
                        BorderThickness="0"
                        IsEnabled="False"
                        Height="194" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,25,15,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <GroupBox 
                                    Header="{Binding FriendlyName, Mode=OneWay}"
                                    Width="380">
                                    <Grid Height="67" Width="Auto">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="3*" />
                                            <ColumnDefinition Width="58*" />
                                            <ColumnDefinition Width="12*" />
                                            <ColumnDefinition Width="295*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="3*" />
                                            <RowDefinition Height="30*" />
                                            <RowDefinition Height="30*"/>
                                            <RowDefinition Height="4*" />
                                        </Grid.RowDefinitions>
                                        <Image
                                            Source="{StaticResource MonitorBlack}"
                                            Height="56" Width="56"
                                            Grid.Column="1" Grid.Row="1" Grid.RowSpan="2"/>
                                        <Image
                                            Source="{Binding CurrentTimePeriodImage,
                                            Mode=OneWay,
                                            UpdateSourceTrigger=PropertyChanged}"
                                            Height="27" Width="32" 
                                            Grid.Row="1" Grid.Column="1" Margin="0,19,0,0" Grid.RowSpan="2" VerticalAlignment="Top"/>

                                        <TextBlock
                                            FontSize="12"
                                            Grid.Column="3" Grid.Row="1">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} : {1} K">
                                                    <Binding
                                                        Source="{StaticResource LocalizationProvider}"
                                                        Path="Instance.Current.ColorTemperature"/>
                                                    <Binding Path="CurrentColorConfiguration.ColorTemperature"
                                                             UpdateSourceTrigger="PropertyChanged"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
        <TextBlock
            FontSize="12"
            Grid.Column="3" Grid.Row="2">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}%">
                    <!--<locl:CurrLocal CurrPath="Brightness"/>-->
                    <Binding Path="CurrentColorConfiguration.Brightness" 
                                Converter="{StaticResource BrightnessToPercent}"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
                                        <!--<TextBlock
                                            FontSize="12"
                                            Grid.Column="3" Grid.Row="2">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} : {1} %">
                                                    <locl:CurrLocal CurrPath="Brightness"/>
                                                    <Binding Path="CurrentColorConfiguration.Brightness" 
                                                                Converter="{StaticResource BrightnessToPercent}"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>-->
                                    </Grid>
                                </GroupBox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button
                        Style="{StaticResource RotaryButton}" 
                        VerticalAlignment="Bottom" HorizontalAlignment="Right"
                        Margin="0,0,10,10"
                        Command="{Binding StopObserver, Mode=OneWay}"
                        Click="{ext:ShowWindow}"
                        CommandParameter="{x:Type views:SettingsWindowView}" Cursor="Hand">
                        <Image 
                            Width="46" Height="46" 
                            Source="{DynamicResource Settings}" 
                            HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    </Button>
                </Grid>
            </Border>
            <StackPanel 
                Grid.Column="1"
                HorizontalAlignment="Right"
                Width="95" 
                FlowDirection="RightToLeft"
                Orientation="Horizontal">
                <Button
                    Style="{StaticResource ButtonAnimatedBackground}"
                    Background="{StaticResource TDeepGray}"
                    Click="{ext:AppShutdown}"
                    Width="30" Height="30" Cursor="Hand">
                    <Image
                        Width="20" Height="20"
                        Source="{StaticResource TurnOff}" >
                    </Image>
                </Button>
                <Button
                    Style="{StaticResource ButtonAnimatedBackground}"
                    Background="{StaticResource TDeepGray}"
                    Click="{ext:ToTray}"
                    Command ="{Binding UnsubscribeEvents, Mode=OneWay}"
                    Width="30" Height="30" Cursor="Hand">
                    <Image
                        Width="20" Height="20"
                        Source="{StaticResource ToTray}" >
                    </Image>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
